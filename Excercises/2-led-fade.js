// Create a variable and initialize the arduino uno board
// http://johnny-five.io/examples/led-fade/

  // Create a standard `led` component instance by creating a variable
  // and initializing the led on pin 11.  Pin 11 supports PWM

  // fade the led in

  // Wait 5 seconds and then fade the led out

